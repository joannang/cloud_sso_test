import {
    observable,
    action,
    runInAction,
    computed,
    makeObservable,
    autorun,
} from 'mobx';


class WalletStore {
    constructor() {
        makeObservable(this, {
            
        })
    }
}

export default WalletStore;